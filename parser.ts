// import {optional as O, or, validate} from 'grammex';

// const _ = /\s*/
// const Number = /-?[0-9]+(\.[0-9]+)/
// const Name = /[a-zA-Z0-9_]/
// const Function = [Name, "(", _,  Expression, _, ")"]
// const Operator = /[+-/^*]+/
// const Term = or([Name, Number, Function, ["(", Expression, ")"]])
// const Operation = or([[Term, _, Operator, _, Expression], Term])
// const Expression = {Term, Operation}

// validate('2 + 3', Expression)
